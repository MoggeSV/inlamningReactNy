import React, { Component } from 'react'
import { connect } from 'react-redux'

const mapStateToProps = (state) => {
return {}
}

const mapDispatchToProps = (dispatch) => {
return {}
}

class componentmall extends Component {

    state = {}


    render() {
        return (
            <div>
                
            </div>
        )
    }
}

export default connect(mapDispatchToProps, mapStateToProps) (componentmall)
